import ChatBox from "../components/ChatBox";
const Chat = () => {
    return (
        <div className="app-container">
            <ChatBox />
        </div>
    );
};

export default Chat;
